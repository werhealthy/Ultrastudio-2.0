"use client";import { useState } from "react";import { Tabs,TabsContent,TabsList,TabsTrigger } from "@/components/ui/tabs";import { Select,SelectContent,SelectItem,SelectTrigger,SelectValue } from "@/components/ui/select";import { PageHeader } from "@/components/PageHeader";import { SearchToolbar } from "@/components/SearchToolbar";import { AssetCard } from "@/components/library/AssetCard";import { CharacterCard } from "@/components/library/CharacterCard";import { CopyLibraryTable } from "@/components/library/CopyLibraryTable";import { assets,characters,copyItems } from "@/lib/mock-data";
export default function LibraryPage(){const [q,setQ]=useState("");return <div className="space-y-5"><PageHeader title="Libreria" description="Asset, personaggi e copy approvati riutilizzabili nelle campagne."/><SearchToolbar value={q} onChange={setQ} placeholder="Cerca in libreria"><Select defaultValue="all"><SelectTrigger className="h-10 w-[160px]"><SelectValue/></SelectTrigger><SelectContent><SelectItem value="all">Tutti gli stati</SelectItem><SelectItem value="approved">Approvati</SelectItem><SelectItem value="draft">Da approvare</SelectItem></SelectContent></Select></SearchToolbar><Tabs defaultValue="images" className="space-y-4"><TabsList className="h-10 rounded-lg bg-muted p-1"><TabsTrigger value="images">Immagini</TabsTrigger><TabsTrigger value="characters">Personaggi</TabsTrigger><TabsTrigger value="copy">Copy</TabsTrigger></TabsList><TabsContent value="images"><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{assets.filter(a=>a.title.toLowerCase().includes(q.toLowerCase())||a.campaign.toLowerCase().includes(q.toLowerCase())).map(a=><AssetCard key={a.id} asset={a}/>)}</div></TabsContent><TabsContent value="characters"><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{characters.map(c=><CharacterCard key={c.id} character={c}/>)}</div></TabsContent><TabsContent value="copy"><CopyLibraryTable items={copyItems}/></TabsContent></Tabs></div>}
