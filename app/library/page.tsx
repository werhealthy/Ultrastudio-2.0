import { Tabs,TabsContent,TabsList,TabsTrigger } from "@/components/ui/tabs";import { SectionHeader } from "@/components/SectionHeader";import { AssetCard } from "@/components/library/AssetCard";import { CharacterCard } from "@/components/library/CharacterCard";import { CopyLibraryTable } from "@/components/library/CopyLibraryTable";import { assets,characters,copyItems } from "@/lib/mock-data";export default function LibraryPage(){return <div><SectionHeader title="Libreria" description="Asset riutilizzabili e approvati per campagne future"/><Tabs defaultValue="images"><TabsList><TabsTrigger value="images">Immagini generate</TabsTrigger><TabsTrigger value="characters">Personaggi</TabsTrigger><TabsTrigger value="copy">Copy</TabsTrigger></TabsList><TabsContent value="images"><div className="grid gap-4 md:grid-cols-4">{assets.map(a=><AssetCard key={a.id} asset={a}/>)}</div></TabsContent><TabsContent value="characters"><div className="grid gap-4 md:grid-cols-4">{characters.map(c=><CharacterCard key={c.id} character={c}/>)}</div></TabsContent><TabsContent value="copy"><CopyLibraryTable items={copyItems}/></TabsContent></Tabs></div>}
