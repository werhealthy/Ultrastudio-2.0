import Link from "next/link";import { Archive, Copy, MoreHorizontal, Send } from "lucide-react";import { Button } from "@/components/ui/button";import { Tabs,TabsContent,TabsList,TabsTrigger } from "@/components/ui/tabs";import { Badge } from "@/components/ui/badge";import { DropdownMenu,DropdownMenuContent,DropdownMenuItem,DropdownMenuTrigger } from "@/components/ui/dropdown-menu";import { CampaignStatusBadge } from "@/components/campaigns/CampaignStatusBadge";import { CampaignOverview } from "@/components/campaigns/CampaignOverview";import { CampaignVisualPanel } from "@/components/campaigns/CampaignVisualPanel";import { CampaignCopyPanel } from "@/components/campaigns/CampaignCopyPanel";import { CampaignFormatsPanel } from "@/components/campaigns/CampaignFormatsPanel";import { campaigns,getCampaignBySlug } from "@/lib/mock-data";import { notFound } from "next/navigation";
type PageProps={params:Promise<{slug:string}>};export function generateStaticParams(){return campaigns.map(c=>({slug:c.slug}))}export async function generateMetadata({params}:PageProps){const {slug}=await params;const c=getCampaignBySlug(slug);return c?{title:c.name,description:c.shortDescription}:{}};
export default async function CampaignDetail({params}:PageProps){const {slug}=await params;const c=getCampaignBySlug(slug);if(!c)notFound();return <div className="space-y-5"><nav className="text-sm text-muted-foreground"><Link href="/campaigns" className="hover:text-primary">Campagne</Link><span className="mx-2">/</span><span className="text-foreground">{c.name}</span></nav><header className="flex flex-col gap-4 border-b pb-5 md:flex-row md:items-end md:justify-between"><div className="space-y-2"><div className="flex flex-wrap items-center gap-2"><CampaignStatusBadge status={c.status}/><Badge variant="secondary">{c.category}</Badge>{c.figma&&<Badge variant="outline">Figma collegato</Badge>}</div><h1 className="text-2xl font-semibold tracking-tight md:text-3xl">{c.name}</h1><p className="text-sm text-muted-foreground">Ultima modifica {c.updated} · consegna {c.date}</p></div><div className="flex gap-2"><Button disabled={!c.figma}><Send className="h-4 w-4"/>Invia a Figma</Button><DropdownMenu><DropdownMenuTrigger asChild><Button variant="outline" aria-label="Azioni secondarie"><MoreHorizontal className="h-4 w-4"/></Button></DropdownMenuTrigger><DropdownMenuContent align="end"><DropdownMenuItem><Copy className="mr-2 h-4 w-4"/>Duplica</DropdownMenuItem><DropdownMenuItem><Archive className="mr-2 h-4 w-4"/>Archivia</DropdownMenuItem></DropdownMenuContent></DropdownMenu></div></header><Tabs defaultValue="overview" className="space-y-5"><TabsList className="h-10 rounded-lg bg-muted p-1"><TabsTrigger value="overview">Overview</TabsTrigger><TabsTrigger value="visual">Visual</TabsTrigger><TabsTrigger value="copy">Copy</TabsTrigger><TabsTrigger value="formats">Formati</TabsTrigger></TabsList><TabsContent value="overview"><CampaignOverview campaign={c}/></TabsContent><TabsContent value="visual"><CampaignVisualPanel campaign={c}/></TabsContent><TabsContent value="copy"><CampaignCopyPanel campaign={c}/></TabsContent><TabsContent value="formats"><CampaignFormatsPanel campaign={c}/></TabsContent></Tabs></div>}
