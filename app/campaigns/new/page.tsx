"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Textarea } from "@/components/ui/textarea";

const steps = ["Punto di partenza", "Informazioni base", "Informazioni campagna", "Visual", "Copy", "Review"];
const slugify = (v: string) => v.toLowerCase().normalize("NFD").replace(/[\u0300-\u036f]/g, "").replace(/[^a-z0-9]+/g, "-").replace(/(^-|-$)/g, "") || "nuova-campagna";

export default function NewCampaignPage() {
  const router = useRouter();
  const [step, setStep] = useState(0);
  const [name, setName] = useState("Nuova campagna UltraDisplay");
  const [request, setRequest] = useState("Brief operativo per nuova campagna");
  const [copy, setCopy] = useState("Headline proposta");
  const finish = () => { const slug = slugify(name); localStorage.setItem("ultradisplay-new-campaign", JSON.stringify({ name, slug, request, copy })); router.push(`/campaigns/${slug}`); };
  return <div className="rounded-[2.5rem] bg-white p-6 shadow-2xl ring-1 ring-blue-100 md:p-8"><div className="mb-8 flex flex-col gap-4 md:flex-row md:items-center md:justify-between"><div><p className="font-black uppercase tracking-[0.25em] text-blue-700">UltraDisplay Wizard</p><h1 className="text-4xl font-black text-slate-950">Crea nuova campagna</h1></div><div className="flex flex-wrap gap-2">{steps.map((s, i) => <button key={s} onClick={() => setStep(i)} className={`rounded-full px-3 py-1 text-xs font-bold ${i === step ? "bg-blue-700 text-white" : "bg-blue-50 text-blue-800"}`}>{i + 1}. {s}</button>)}</div></div><section className="min-h-[26rem] rounded-[2rem] bg-blue-50 p-6"><h2 className="text-2xl font-black text-slate-950">{step === 0 ? "Da dove vuoi iniziare?" : steps[step]}</h2>{step === 0 && <div className="mt-6 grid gap-4 md:grid-cols-3">{["Da zero", "Da un brief esistente", "Da una campagna passata"].map(o => <button className="rounded-2xl border border-blue-100 bg-white p-6 text-left font-bold shadow-sm hover:border-blue-600" key={o}>{o}</button>)}</div>}{step === 1 && <div className="mt-6 grid gap-4 md:grid-cols-2"><Input value={name} onChange={e => setName(e.target.value)} placeholder="Nome"/><Input placeholder="Stato" defaultValue="Bozza"/><Textarea value={request} onChange={e => setRequest(e.target.value)} placeholder="Richiesta"/><Input placeholder="Deliverables" defaultValue="Hero web, Display, Stories"/><Input type="date"/><Input placeholder="Figma collegato" defaultValue="Da collegare"/></div>}{step === 2 && <div className="mt-6 grid gap-4 md:grid-cols-2"><Textarea placeholder="Background"/><Textarea placeholder="Descrizione offerta"/><Textarea placeholder="Target"/><Textarea placeholder="Benefit / Reason Why"/></div>}{step === 3 && <div className="mt-6 grid gap-4 md:grid-cols-2"><div className="rounded-2xl bg-white p-6">Visual selezionato / libreria</div><div className="grid gap-3"><Button>Carica asset</Button><Button variant="outline">Genera con AI</Button><Button variant="outline">Modifica con AI</Button></div></div>}{step === 4 && <div className="mt-6 grid gap-4 md:grid-cols-2"><Textarea value={copy} onChange={e => setCopy(e.target.value)} placeholder="Headline"/><Textarea placeholder="Subheadline"/><Input placeholder="CTA"/><Input placeholder="Prezzo"/><Textarea placeholder="Disclaimer"/></div>}{step === 5 && <div className="mt-6 rounded-2xl bg-white p-6"><b>{name}</b><p className="mt-2 text-slate-600">{request}</p><p className="mt-2 text-slate-600">Copy: {copy}</p><p className="mt-4 rounded-xl bg-amber-50 p-3 text-sm text-amber-800">Warning: collega Figma prima dell’invio finale.</p></div>}</section><div className="mt-6 flex justify-between"><Button variant="outline" disabled={step === 0} onClick={() => setStep(s => s - 1)}>Indietro</Button>{step < 5 ? <Button className="bg-blue-700 hover:bg-blue-800" onClick={() => setStep(s => s + 1)}>Avanti</Button> : <div className="flex gap-2"><Button variant="outline">Salva come bozza</Button><Button className="bg-blue-700 hover:bg-blue-800" onClick={finish}>Crea campagna</Button><Button className="bg-red-600 hover:bg-red-700" onClick={finish}>Crea e apri campagna</Button></div>}</div></div>;
}
