"use client";import { useState } from "react";import { useRouter } from "next/navigation";import { Button } from "@/components/ui/button";import { Card,CardContent } from "@/components/ui/card";import { Input } from "@/components/ui/input";import { Textarea } from "@/components/ui/textarea";import { PageHeader } from "@/components/PageHeader";
const steps=["Punto di partenza","Informazioni base","Informazioni campagna","Visual","Copy","Review"];const slugify=(v:string)=>v.toLowerCase().normalize("NFD").replace(/[\u0300-\u036f]/g,"").replace(/[^a-z0-9]+/g,"-").replace(/(^-|-$)/g,"")||"nuova-campagna";
export default function NewCampaignPage(){const router=useRouter();const [step,setStep]=useState(0);const [name,setName]=useState("Nuova campagna UltraDisplay");const [request,setRequest]=useState("Brief operativo per nuova campagna");const [copy,setCopy]=useState("Headline proposta");const finish=()=>{router.push(`/campaigns/${slugify(name)}`)};return <div className="space-y-5"><PageHeader title="Crea nuova campagna" description="Wizard mock per raccogliere brief, asset e copy in modo guidato."/><Card><CardContent className="space-y-6 p-5"><div className="flex flex-wrap gap-2">{steps.map((s,i)=><button key={s} onClick={()=>setStep(i)} className={`h-9 rounded-full px-3 text-xs font-medium ${i===step?"bg-primary text-primary-foreground":"bg-muted text-muted-foreground hover:text-foreground"}`}>{i+1}. {s}</button>)}</div><section className="min-h-[22rem] rounded-xl border bg-muted/40 p-5"><h2 className="section-title">{step===0?"Da dove vuoi iniziare?":steps[step]}</h2>{step===0&&<div className="mt-5 grid gap-3 md:grid-cols-3">{["Da zero","Da un brief esistente","Da una campagna passata"].map(o=><button className="rounded-xl border bg-card p-5 text-left text-sm font-medium hover:border-primary" key={o}>{o}</button>)}</div>}{step===1&&<div className="mt-5 grid gap-4 md:grid-cols-2"><Input value={name} onChange={e=>setName(e.target.value)} placeholder="Nome"/><Input placeholder="Stato" defaultValue="Bozza"/><Textarea className="md:col-span-2" value={request} onChange={e=>setRequest(e.target.value)} placeholder="Richiesta"/><Input placeholder="Deliverables" defaultValue="Hero web, Display, Stories"/><Input type="date"/></div>}{step===2&&<div className="mt-5 grid gap-4 md:grid-cols-2"><Textarea placeholder="Background"/><Textarea placeholder="Descrizione offerta"/><Textarea placeholder="Target"/><Textarea placeholder="Benefit / Reason Why"/></div>}{step===3&&<div className="mt-5 grid gap-4 md:grid-cols-2"><div className="rounded-xl border bg-card p-5 text-sm text-muted-foreground">Visual selezionato / libreria</div><div className="grid gap-2"><Button variant="outline">Carica asset</Button><Button variant="outline">Genera con AI</Button></div></div>}{step===4&&<div className="mt-5 grid gap-4 md:grid-cols-2"><Textarea value={copy} onChange={e=>setCopy(e.target.value)} placeholder="Headline"/><Textarea placeholder="Subheadline"/><Input placeholder="CTA"/><Input placeholder="Prezzo"/></div>}{step===5&&<div className="mt-5 rounded-xl border bg-card p-5"><b>{name}</b><p className="mt-2 text-sm text-muted-foreground">{request}</p><p className="mt-2 text-sm text-muted-foreground">Copy: {copy}</p><p className="mt-4 rounded-lg bg-amber-50 p-3 text-sm text-amber-800">Warning: collega Figma prima dell’invio finale.</p></div>}</section><div className="flex justify-between"><Button variant="outline" disabled={step===0} onClick={()=>setStep(s=>s-1)}>Indietro</Button>{step<5?<Button onClick={()=>setStep(s=>s+1)}>Avanti</Button>:<div className="flex gap-2"><Button variant="outline">Salva come bozza</Button><Button onClick={finish}>Crea campagna</Button></div>}</div></CardContent></Card></div>}
