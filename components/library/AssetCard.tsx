import { Button } from "@/components/ui/button";import { Badge } from "@/components/ui/badge";export function AssetCard({asset}:any){return <div className="overflow-hidden rounded-lg border bg-card"><img src={asset.src} className="h-36 w-full object-cover" alt=""/><div className="space-y-2 p-4"><b>{asset.title}</b><p className="text-sm text-muted-foreground">{asset.campaign} · {asset.date}</p><Badge variant="secondary">{asset.status}</Badge><div className="flex flex-wrap gap-1">{asset.tags.map((t:string)=><Badge key={t} variant="outline">{t}</Badge>)}</div><Button size="sm" variant="outline">Usa in campagna</Button></div></div>}
