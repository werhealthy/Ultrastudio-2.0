"use client";import { useState } from "react";import type { Campaign } from "@/lib/mock-data";import { Button } from "@/components/ui/button";import { Input } from "@/components/ui/input";export function CampaignCopyPanel({campaign}:{campaign:Campaign}){const [copy,setCopy]=useState(campaign.copy);const update=(k:keyof typeof copy,v:string)=>setCopy({...copy,[k]:v});const alt=(k:keyof typeof copy)=>update(k,copy[k]+" · nuova variante");const short=(k:keyof typeof copy)=>update(k,copy[k].slice(0,Math.max(12,Math.floor(copy[k].length*.7))));return <div className="space-y-4">{(Object.keys(copy) as (keyof typeof copy)[]).map(k=><div key={k} className="rounded-lg border bg-card p-4"><div className="mb-2 flex items-center justify-between"><label className="capitalize font-medium">{k}</label><span className="text-xs text-muted-foreground">{copy[k].length} caratteri</span></div><Input value={copy[k]} onChange={e=>update(k,e.target.value)}/><div className="mt-3 flex gap-2"><Button size="sm" variant="outline" onClick={()=>alt(k)}>Genera alternative</Button><Button size="sm" variant="outline" onClick={()=>short(k)}>Accorcia</Button><Button size="sm" variant="ghost">Cerca in libreria</Button></div></div>)}</div>}
