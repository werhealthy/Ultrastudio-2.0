"use client";import { useState } from "react";import { MoreHorizontal } from "lucide-react";import type { Campaign } from "@/lib/mock-data";import { Button } from "@/components/ui/button";import { Input } from "@/components/ui/input";import { DropdownMenu,DropdownMenuContent,DropdownMenuItem,DropdownMenuTrigger } from "@/components/ui/dropdown-menu";
export function CampaignCopyPanel({campaign}:{campaign:Campaign}){const [copy,setCopy]=useState(campaign.copy);const update=(k:keyof typeof copy,v:string)=>setCopy({...copy,[k]:v});return <div className="surface space-y-4 p-4">{(Object.keys(copy) as (keyof typeof copy)[]).map(k=><div key={k} className="rounded-lg border bg-card p-4"><div className="mb-2 flex items-center justify-between"><label className="text-sm font-medium capitalize" htmlFor={`copy-${k}`}>{k}</label><span className="meta-text">{copy[k].length} caratteri</span></div><div className="flex gap-2"><Input id={`copy-${k}`} value={copy[k]} onChange={e=>update(k,e.target.value)}/><DropdownMenu><DropdownMenuTrigger asChild><Button variant="outline" aria-label={`Azioni AI ${k}`}><MoreHorizontal className="h-4 w-4"/></Button></DropdownMenuTrigger><DropdownMenuContent align="end"><DropdownMenuItem onClick={()=>update(k,copy[k]+" · nuova variante")}>Genera alternative</DropdownMenuItem><DropdownMenuItem onClick={()=>update(k,copy[k].slice(0,Math.max(12,Math.floor(copy[k].length*.7))))}>Accorcia</DropdownMenuItem><DropdownMenuItem>Cerca in libreria</DropdownMenuItem></DropdownMenuContent></DropdownMenu></div></div>)}</div>}
