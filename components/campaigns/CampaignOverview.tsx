import type { Campaign } from "@/lib/mock-data";import { EditableField } from "@/components/EditableField";import { Alert } from "@/components/ui/alert";export function CampaignOverview({campaign}:{campaign:Campaign}){return <div className="grid gap-4 lg:grid-cols-2"><EditableField label="Nome" value={campaign.name}/><EditableField label="Stato" value={campaign.status}/><EditableField label="Richiesta" value={campaign.request} long/><EditableField label="Deliverables" value={campaign.deliverables.join(', ')}/><EditableField label="Data" value={campaign.date}/><EditableField label="Allegati" value={campaign.attachments.join(', ')||'Nessun allegato'}/><Alert>{campaign.figma?'Figma collegato e pronto alla sincronizzazione.':'Figma non collegato per questa campagna.'}</Alert><div className="lg:col-span-2 grid gap-4 md:grid-cols-2"><EditableField label="Background" value={campaign.background} long/><EditableField label="Descrizione offerta" value={campaign.offer} long/><EditableField label="Target" value={campaign.target} long/><EditableField label="Benefit / Reason Why" value={campaign.benefits} long/></div></div>}
