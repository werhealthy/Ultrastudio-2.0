import type { Campaign } from "@/lib/mock-data";import { EditableField } from "@/components/EditableField";import { Alert } from "@/components/ui/alert";import { Card,CardContent,CardHeader,CardTitle } from "@/components/ui/card";import { Progress } from "@/components/ui/progress";
export function CampaignOverview({campaign}:{campaign:Campaign}){const missing=[!campaign.figma&&"Figma da collegare",campaign.formats.some(f=>f.warning)&&"Warning formati",campaign.visuals.length===0&&"Visual mancante"].filter(Boolean);const progress=missing.length?70:100;return <div className="grid gap-5 lg:grid-cols-[1fr_340px]"><Card><CardHeader><CardTitle className="text-base">Informazioni campagna</CardTitle></CardHeader><CardContent className="grid gap-4 md:grid-cols-2"><EditableField label="Richiesta" value={campaign.request} long/><EditableField label="Deliverables" value={campaign.deliverables.join(", ")}/><EditableField label="Data" value={campaign.date}/><EditableField label="Allegati" value={campaign.attachments.join(", ")||"Nessun allegato"}/><EditableField label="Background" value={campaign.background} long/><EditableField label="Offerta" value={campaign.offer} long/><EditableField label="Target" value={campaign.target} long/><EditableField label="Benefit" value={campaign.benefits} long/></CardContent></Card><Card><CardHeader><CardTitle className="text-base">Stato avanzamento</CardTitle></CardHeader><CardContent className="space-y-4"><div><div className="mb-2 flex justify-between text-sm"><span>Completezza</span><span>{progress}%</span></div><Progress value={progress}/></div><Alert>{campaign.figma?"Figma collegato e pronto alla sincronizzazione.":"Figma non collegato per questa campagna."}</Alert><div className="rounded-lg bg-muted p-3 text-sm"><p className="font-medium">Contenuti mancanti</p><p className="mt-1 text-muted-foreground">{missing.join(", ")||"Nessuno"}</p></div><div><p className="mb-2 text-sm font-medium">Ultime attività</p><ul className="space-y-2 text-sm text-muted-foreground">{campaign.activity.map(item=><li key={item}>• {item}</li>)}</ul></div></CardContent></Card></div>}
