import type { Campaign } from "@/lib/mock-data";import { Button } from "@/components/ui/button";import { Badge } from "@/components/ui/badge";import { Progress } from "@/components/ui/progress";export function CampaignFormatsPanel({campaign}:{campaign:Campaign}){return <div className="space-y-3">{campaign.formats.map(f=><div key={f.name} className="rounded-lg border bg-card p-4"><div className="flex items-center justify-between"><div><b>{f.name}</b><p className="text-sm text-muted-foreground">{f.figma||'File Figma non ancora generato'}</p></div><Badge className={f.progress===100?'bg-emerald-100 text-emerald-800':'bg-amber-100 text-amber-800'}>{f.progress===100?'Pronto':'Mancante'}</Badge></div><div className="mt-3"><Progress value={f.progress}/></div>{f.warning&&<p className="mt-2 text-sm text-orange-700">Warning: {f.warning}</p>}</div>)}<Button>Invia a Figma</Button></div>}
