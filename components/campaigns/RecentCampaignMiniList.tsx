import Link from "next/link";import { ArrowUpRight } from "lucide-react";import { CampaignStatusBadge } from "./CampaignStatusBadge";import type { Campaign } from "@/lib/mock-data";
export function RecentCampaignMiniList({ campaigns }: { campaigns: Campaign[] }) {return <div className="divide-y">{campaigns.slice(0,3).map(c=><Link href={`/campaigns/${c.slug}`} key={c.id} className="flex items-center justify-between gap-3 py-3 text-sm hover:text-primary"><span className="min-w-0"><span className="block truncate font-medium">{c.name}</span><span className="meta-text">Modificata {c.updated}</span></span><span className="flex items-center gap-2"><CampaignStatusBadge status={c.status}/><ArrowUpRight className="h-4 w-4" aria-hidden="true"/></span></Link>)}</div>}
